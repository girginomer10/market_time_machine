# Agent Handoff

## 2026-07-21 11:42 +03 - Codex (Kimi deep-review remediation)

- Task: Verified Kimi's repo-wide review against the live V2 worktree and fixed the reproducible execution, persistence, archive, validation, and UI-state defects without changing intentional compatibility contracts.
- Changed: Hardened market-fill price validation, percent-volatility normalization, deferred stop pricing, zero-volume volume-limited order handling, OCO sibling persistence, restored-finished session validation, journal/decision text bounds, broker locking, and forced-liquidation state. Added optimistic same-run cross-tab session conflict protection, canonical archive storage refresh/fresh export, explicit archive read failures, post-conflict ledger capping, and library-safe ticket/journal drafts. Tightened scenario enum/event validation, activated built-in track catalog validation at runtime, corrected chart event placement and broker documentation, and clarified the intentional author-label/content-hash `dataVersion` contract. Added focused regressions across all affected layers.
- Verified: `npm run check` passed lint, 826/826 tests across 50 files, TypeScript, production build, bundle-license, and PWA gates. `git diff --check` passed before this entry. `npm audit --audit-level=low` separately reported one high-severity advisory in the development-only ESLint dependency chain (`brace-expansion <1.1.16`); `npm audit fix --dry-run` showed the fix would also expand platform-optional lockfile entries, so dependency mutation was left out of this scoped remediation.
- Memory: No repo semantic-memory system is installed. Intentional contracts retained: imported scenarios require an author `dataVersion` label but receive a content-derived SHA-256 identity; legacy/import compatibility assessment codes remain accepted where already documented; stable opaque event IDs were not renamed.
- Next: Run a manual two-tab browser smoke test for session conflict messaging and archive refresh/export, then decide separately whether to update the ESLint dependency tree for the current audit advisory. Confirm the pushed CI result before tagging a release.

## 2026-07-16 06:53 +03 - Codex (V2 completion paused for shutdown)

- Task: Stopped immediately at the user's request so the computer can be shut down. Interrupted the archive-integrity follow-up and the read-only release/data miss-hunt, then stopped both local preview servers. No Vite, Vitest, preview, test, or full-check process remains active. No commit or push was made.
- Changed: Preserve the shared worktree exactly as-is. Branch `main`, `HEAD`, and `origin/main` remain at `3d7998d`; the tree has 83 tracked modifications and nine intended untracked source/test files. Since the 06:30 checkpoint, completed assessment aggregates are recomputed from their recorded plan/checkpoint/event/rule evidence before ledger, evidence, coach, or track credit; legacy fingerprint-less records remain readable but do not count as current measured evidence. The scenario registry now keeps private mutable storage behind a read-only proxy and recursively freezes built-in and imported scenarios so assigned content hashes cannot drift by reference. Product/privacy/V2 docs now state that editable local archives prove bounded internal consistency, not cryptographic authorship, certification, or anti-cheat. The two agents interrupted in this stop did not leave an additional follow-up patch in their assigned archive/release slices.
- Verified: The assessment slice passed 139/139 focused tests; the scenario-registry slice passed 55/55; the integrated focused run passed 194/194. A clean `npm ci`, `npm run check` (50 files, 812/812 tests plus lint, TypeScript, production build, bundle-license and PWA gates), `npm audit --audit-level=low` (zero vulnerabilities), and `git diff --check` passed. Live ECB EUR/GBP and EUR/USD reimports produced 152 and 104 observations and were byte-identical to the committed fixtures. Browser acceptance is only partial: a clean `localhost:4174` run reached the guided EUR/GBP drill, completed the plan and trade, and recorded the first two event checkpoints; desktop/mobile, refresh/resume, completed report/evidence/track, console, and offline-reopen proof still remain. `git diff --check` was rerun successfully at shutdown.
- Memory: The final UI/persistence audit found eight unresolved product-integrity issues: (1) active-session persistence is last-writer-wins across tabs; (2) the archive mutation fallback is not atomic when Web Locks are unavailable; (3) archive import applies the 250-entry cap before removing cross-layer conflicts and can evict unrelated evidence; (4) an allowed journal note over 100,000 characters can create an invalid finished run and unusable recovery export; (5) archive read failures can be presented as healthy empty history; (6) history/export state does not refresh after another tab writes and export can use stale React state; (7) a forged restored session can claim `finished` at the first candle and be auto-archived; and (8) opening the library unmounts and loses unsent order-plan/journal drafts even when the user chooses Continue. The read-only final release/data miss-hunt was interrupted before returning a report.
- Next: Resume without resetting or discarding the diff. First fix the archive-layer items in `practiceArchive.ts`, `practiceArchiveStorage.ts`, `practiceArchiveEnvelope.ts`, and `runHistory.ts`: fail closed without Web Locks (or provide a truly atomic fallback), resolve conflicts before caps, bound/sanitize internal text, and surface read errors. Then harden `sessionStore.ts` against stale-tab overwrites and forged early `finished` restores. In `App.tsx` and the affected editors, reload canonical archive state on storage changes, export from a fresh locked read, globally surface archive-read failures, and preserve unsent drafts while the library is open. Add interleaving/two-tab/oversized-note/forged-restore/draft-preservation regressions. Complete the release/data miss-hunt, rerun focused and full release gates, rebuild preview, and perform the entire desktop/mobile/refresh/resume/report/evidence/track/console/offline browser pass from a clean origin. Only then update release docs, add a final handoff, commit intended files to `main`, push `origin/main`, and confirm CI green. Keep ignored/generated `dist/`, dependencies, build-info files, credentials, and `src/data/scenarios/sp500-covid-2020-fred/` out of the commit.

## 2026-07-16 06:30 +03 - Codex (V2 completion stopped for shutdown)

- Task: Stopped immediately at the user's request so the computer can be shut down. All implementation and audit agents have completed, and no project Vite, Vitest, preview, test, or full-check process remains active; only the Codex desktop kernel is present. No commit or push was made.
- Changed: Preserve the current shared worktree exactly as-is. Branch `main`, `HEAD`, and `origin/main` remain at `3d7998d`; the tree has 83 tracked modifications and nine untracked source/test files. Since the 06:01 checkpoint, authoritative assessment display was added so partial or stale schedules cannot appear as current scores in Run History, Post-game sharing, or Practice Coach; imported scenarios now receive an app-derived SHA-256 over the full replay contract including authored drills and duplicate stored IDs fail closed; active-session restore/replacement races and meaningful-progress detection were hardened; persistence failures became globally visible; importer locking, recovery, atomic staging, `fsync`, and strict timestamp validation were completed; and the product/privacy/architecture/data documentation was reconciled with these contracts.
- Verified: The latest focused runs passed 72/72 authoritative score/history/coach tests, 26/26 Post-game tests, 53/53 scenario identity tests, 94/94 importer durability tests, 101/101 App/session-integrity tests, and a combined 367/367 integration slice. TypeScript, scoped ESLint, and diff checks passed after the implementation slices. These are not final release proof: the current tree has not received a fresh clean `npm ci`, full `npm run check`, dependency audit, ECB byte-identical reproduction, final browser acceptance, commit, push, or CI confirmation.
- Memory: Two audit findings remain open. First, a ledger-only completed assessment can claim contradictory scores such as zero linked events with a 100 event-linkage component because component scores are not yet recomputed from the recorded aggregate counts. Add a deterministic completed-assessment aggregate-consistency validator in `src/domain/practice/drills.ts` using the rubric fingerprint, enforce it in ledger parsing and direct evidence/track score paths, and add ledger/evidence/track/coach regressions. Document that editable local archives are consistency-validated self-reported evidence, not cryptographically authentic certification or anti-cheat. Second, a registered imported scenario remains mutable by reference after its content hash is assigned. Deep-freeze registry packages, keep mutation behind an internal registry with a read-only public surface, and add nested-mutation/hash-stability regressions.
- Next: Resume with those two findings only; do not reset or discard the shared diff. Then rerun the focused integrity suites plus TypeScript/lint/diff checks, followed by clean install, full release checks, dependency audit, ECB reproducibility, and bundle/PWA gates. Rebuild preview only for the final desktop/mobile/refresh/resume/console/offline browser pass. After all gates pass, update the release checklist and this handoff, commit intended files to `main`, push `origin/main`, and confirm CI green. Keep ignored/generated `dist/`, dependencies, build-info files, credentials, and `src/data/scenarios/sp500-covid-2020-fred/` out of the commit.

## 2026-07-16 06:01 +03 - Codex (V2 completion stopped for shutdown)

- Task: Stopped immediately at the user's request so the computer can be shut down. Interrupted all three active read-only final audits (`final_identity_misshunt`, `final_importer_misshunt`, and `final_product_contract`) and stopped the local preview server. No project Vite, Vitest, browser-acceptance, or check process remains active; only the Codex desktop kernel is present. No commit or push was made.
- Changed: Preserve the current shared worktree exactly as-is. Branch `main`, `HEAD`, and `origin/main` remain at `3d7998d`; the tree has 83 tracked modifications and eight untracked source/test files. Since the 05:31 checkpoint, the tree gained completed archive-busy controls, report-save recovery gating, restore replacement confirmation, exact archived-drill replay context, verified active-session deletion feedback, safe same-ID imported-scenario rejection, transactional importer recovery/stale-lock scaffolding, strict timezone requirements, corrected BTC Fed-event time/versioning, updated ECB replay-window provenance and hashes, and reconciled release/privacy/V2 documentation. The latest root patch now validates assessment counts against the complete authoritative checkpoint schedule before ledger, track, or evidence credit and restores cross-scenario competency grouping by removing the drill-specific schedule fingerprint from the evidence claim key. That last evidence/schedule patch is untested and must be treated as in progress.
- Verified: Before the last audit-driven patches, a clean Node 22/npm 10 install and `npm run check` passed lint, TypeScript, production build, bundle/PWA gates, and 764/764 tests across 50 files; `npm audit --audit-level=low` reported zero vulnerabilities; `git diff --check` passed; and live ECB reimports were byte-identical at 152 EUR/GBP and 104 EUR/USD observations. Focused importer/scenario/version coverage also reached 95/95, and the official Fed release independently supports the corrected `21:00Z` timestamp. These results are stale for the current tree because the schedule/evidence files changed afterward. No final focused rerun, full check, browser acceptance, commit, push, or CI confirmation exists for the exact stopped state.
- Memory: Resume with the current diff; do not reset or discard shared edits. Confirmed open product gaps are: (1) assessment-only history/report fallback can still display a self-consistent partial-schedule score; (2) Practice Coach refresh copy can surface a non-authoritative assessment score; (3) archived replay needs a fresh-unassessed drift fallback and key-order-independent drill identity; (4) imported scenarios need an app-derived full replay-contract identity that includes scenario-authored drills, plus duplicate stored-ID startup safety; (5) `sessionHasProgress` must include initial drill plans, checkpoint responses, and rule violations, restore must re-check progress after awaiting file reads, and local persistence failures need global save-health/recovery UI; (6) importer stale-lock takeover has a two-contender race, manifest-less transaction recovery can delete arbitrary contents, local/retrieval timestamps still need strict ISO calendar validation, and true power-loss durability may require flush/fsync; and (7) docs must say schedules gate evidence validity but competency claims group by competency and rubric identity, not drill-specific schedule.
- Next: First rerun the focused drills/ledger/tracks/evidence suite to validate the last patch. Then close the open report/coach/replay/imported-scenario/persistence/importer gaps with regressions, reconcile docs, and run a fresh clean install, full `npm run check`, dependency audit, ECB byte-identical reproduction, bundle/PWA gates, and `git diff --check`. Rebuild and restart preview only for final desktop/mobile/refresh/resume/console/offline browser acceptance. After all gates pass, add a final handoff, commit intended files to `main`, push `origin/main`, and confirm CI green. Keep ignored/generated `dist/`, dependencies, build-info files, credentials, and `src/data/scenarios/sp500-covid-2020-fred/` out of the commit.

## 2026-07-16 05:31 +03 - Codex (V2 completion stopped for shutdown)

- Task: Stopped immediately at the user's request so the computer can be shut down. All three final-audit agents are no longer running: the release and UI audits completed, while the history/identity audit ended after returning its decisive forged-checkpoint repro. No Vite, Vitest, preview, or full-check process remains active. No commit or push was made.
- Changed: Preserve the current shared worktree exactly as-is. Branch `main`, `HEAD`, and `origin/main` remain at `3d7998d`; the tree now has 83 tracked modifications and eight untracked source/test files. Since the 05:07 checkpoint, legacy practice-archive migration was moved under the archive mutation lock with a non-mutating initial read and merge-on-first-persist protection. Practice assessments, track units, evidence, coaching, archives, and App wiring now carry an authoritative checkpoint-schedule fingerprint so a self-declared partial drill cannot earn full credit. UI hardening is partially implemented: destructive confirmations gain a busy state; session-file restore is routed through replacement confirmation when progress exists; post-game navigation/export is blocked while an archive save is pending or failed; a successful recovery download clears the unsafe-to-leave warning. The last UI edits are unverified and should be treated as work in progress.
- Verified: Legacy migration/App coverage passed 66/66 focused tests. The schedule-identity evidence/track/coach/archive/App slice passed 119/119. TypeScript and scoped lint passed before the latest UI patches. A later audit snapshot passed 142/146 UI-focused tests; three App fixtures and one Run History fixture were still red, and the Run History schedule fixture was patched afterward but not rerun. The release audit's earlier clean install/check passed 743 tests and found zero dependency vulnerabilities, but root changed files after that run, so it is not final proof for the current tree. There is no fresh combined TypeScript run, full `npm run check`, browser acceptance, commit, push, or CI result.
- Memory: Resume with the unfinished product gaps, not a bug-only pass. First compile and rerun the current UI/history/App slices. Finish archive-busy controls in Run History; exact archived-drill replay identity; honest browser-save/delete results; required `dataVersion` plus safe same-ID user-scenario replacement; and regressions for confirmation busy state, failed report save/recovery, and session-restore replacement. Then address the release audit: reject timezone-less local OHLCV timestamps; make paired importer publication recover from stale locks/interrupted writes; correct the BTC 2020-03-15 Fed event from `22:00Z` to `21:00Z` and bump its data version; disclose ECB's derived `00:00Z-15:00Z` replay window; update the stale service-worker cache-version documentation. Reconcile privacy/V2/release docs for schedule fingerprints and locking.
- Next: Start by inspecting the current diff and running TypeScript plus the focused App, Run History, Post-game, archive, evidence, track, coach, and session suites. Complete the remaining UI/data/importer fixes with regressions, then run clean `npm ci`, full `npm run check`, `npm audit --audit-level=low`, ECB byte-identical reproducibility, bundle/PWA gates, and `git diff --check`. Finish real desktop/mobile/refresh/resume/console/offline browser acceptance. Only after all gates pass, update this handoff/release checklist, commit intended files to `main`, push `origin/main`, and confirm CI green. Keep ignored/generated `dist/`, dependencies, build-info files, credentials, and `src/data/scenarios/sp500-covid-2020-fred/` out of the commit.

## 2026-07-16 05:07 +03 - Codex (V2 completion stopped for shutdown)

- Task: Stopped immediately at the user's request so the computer can be shut down. Interrupted all three active read-only final-audit agents (`final_history_identity_audit`, `final_release_audit`, and `final_ui_state_audit`). No project Vite, Vitest, preview, or check process remains active; only the Codex desktop kernel is present. No commit or push was made.
- Changed: Preserve the shared worktree exactly as-is. Branch `main`, `HEAD`, and `origin/main` remain at `3d7998d`; the tree has 82 tracked modifications and eight untracked source/test files. Since the 04:38 checkpoint, the history validator now recomputes modern assessments and enforces caps/exact identity, archive persistence has a revisioned canonical envelope plus CAS/rebase handling and Web Lock coverage for App mutations, replay/reset preserves exact broker and mode context, Surprise symbols are masked before render, active-drill plan validation is enforced, and report archive failure now has retry plus a genuinely importable recovery download. Treat the complete diff as unfinished and uncommitted.
- Verified: The latest history/archive slices passed up to 109 focused tests; the latest App, post-game report, and archive-lock integration slice passed 88/88; the Trade Panel/session/report slice passed 91/91; TypeScript and scoped lint passed before the final small edits. Earlier importer/release work passed 72/72. These are focused results only: there is no fresh full `npm run check`, clean-install proof, final audit, browser acceptance, reproducibility proof, commit, push, or CI result for the current tree.
- Memory: Two confirmed high-severity product-integrity gaps remain. First, automatic legacy archive migration can still write outside the Web Lock and may overwrite a newer canonical archive; resume by making initial App loading non-migrating, performing migration under `withPracticeArchiveMutationLock`, and defensively merging with stored canonical data on the first persistence attempt, with deterministic race tests. Second, an imported/self-declared one-checkpoint assessment can satisfy a track whose authoritative EUR/GBP drill actually has five checkpoints; require the complete current scenario/drill schedule for both full-run and ledger-only imported evidence before granting track/evidence credit. The interrupted audit agents did not return final reports, so rerun their miss-hunts. `docs/privacy-and-local-data.md` also still contains stale “no cross-tab locking / last-writer-wins” wording.
- Next: Resume with the two confirmed integrity blockers and regressions, then rerun the three final audits and address only reproducible findings. Run focused suites, clean `npm ci`, full `npm run check`, `npm audit --audit-level=low`, ECB byte-identical reproducibility, bundle/PWA gates, and `git diff --check`; complete desktop/mobile/refresh/resume/console/offline browser acceptance; reconcile privacy/release/V2 docs and this handoff; only then commit intended files to `main`, push `origin/main`, and confirm CI green. Keep ignored/generated `dist/`, dependencies, build-info files, credentials, and `src/data/scenarios/sp500-covid-2020-fred/` out of the commit.

## 2026-07-16 04:38 +03 - Codex (V2 completion stopped for shutdown)

- Task: Stopped immediately at the user's request. Interrupted the two active history/archive implementation agents and confirmed that no project Vite, Vitest, preview, or check process remains active. No commit or push was made.
- Changed: Preserve the shared worktree exactly as-is. Branch `main`, `HEAD`, and `origin/main` remain at `3d7998d`; the tree has 82 tracked modifications and eight untracked source/test files. Since the 04:06 checkpoint, release/import hardening now covers symlink-safe roots and bundle traversal, concurrent no-overwrite publication, atomic ECB writes, tiny-tick precision, canonical-root-only service-worker shell refreshes, and explicit FRED derived-session/early-close disclosure. Root also began two UI fixes: active-drill plan validation in `TradePanel` and broker/mode-preserving replay/reset support in `sessionStore`. Treat those UI edits as unfinished. The interrupted agents may have left partial edits in `runHistory.ts`/tests and `practiceArchiveStorage.ts`/tests plus `practiceArchiveEnvelope.ts`; inspect before continuing.
- Verified: The combined product-integrity slice passed 249/249 focused tests; scenario/version/broker suites passed 59/59; bundle symlink regressions passed 3/3; the release/data slice passed 71/71; and the combined importer slice, including atomic ECB publication, passed 72/72. `git diff --check` passes at this stop point. These are slice results only: the current tree has no fresh full `npm run check`, final reproducibility proof, live browser acceptance, commit, push, or CI result.
- Memory: The final UI audit found open product work around exact broker-preserving history replay, reset preserving Surprise/broker identity, raw-symbol masking, retryable/importable archive recovery, stale coach assignments, single-run delete confirmation, and three wording/fallback defects. The identity audit additionally found legacy 250-entry wrapper validation, recomputed assessment integrity, divergent duplicate-run handling, archive cross-tab concurrency, and exact-context history comparison gaps. Keep generated/ignored `dist/`, dependencies, build-info files, credentials, and `src/data/scenarios/sp500-covid-2020-fred/` out of any future commit.
- Next: Resume by inspecting the interrupted history/archive diffs and compiling before changing them. Finish and test the in-progress `TradePanel` and `sessionStore` work, then implement the remaining UI/integrity findings. Run focused suites followed by clean `npm ci`, full `npm run check`, audit, ECB byte-for-byte reproducibility, PWA/bundle gates, and `git diff --check`; complete clean desktop/mobile/refresh/resume/console/offline browser acceptance; reconcile release docs; only then commit intended files to `main`, push `origin/main`, and confirm CI green.

## 2026-07-16 04:06 +03 - Codex (V2 completion stopped for shutdown)

- Task: Stopped immediately at the user's request. Interrupted the only running release/data hardening agent before it changed its assigned files. The session/history agent had just completed and returned its fixes. No project Vite, Vitest, preview, test, or check process remains active; only the Codex desktop kernel is present.
- Changed: Preserve the shared worktree exactly as-is. Branch `main`, `HEAD`, and `origin/main` are still at `3d7998d`; the tree has 78 tracked modifications and seven untracked source/test files. Since the 03:43 checkpoint, the tree gained exact broker mode/full-config identity for practice-track units and credit, exact track preparation through Scenario Library/App, stricter coaching/evidence comparability, no-trade milestone exclusion, broker-safe legacy session restore, locked-mode broker-label validation, and report sanitization before archive wrapper counts. No commit or push was made.
- Verified: Track/UI integration passed 59/59 focused tests; coach/evidence passed 44/44; the completed session/history slice passed 68/68 plus TypeScript, scoped lint, and `git diff --check`. A preliminary `npm audit --audit-level=low` reported zero vulnerabilities. These are focused results, not a final release proof: the latest combined tree has no fresh full `npm run check`, final importer/reproducibility run, second product audit, live desktop/mobile/refresh/resume/console/offline browser acceptance, commit, push, or CI result.
- Memory: The release/data audit still has five unimplemented findings because its fix agent was stopped before editing: symlink-safe importer output roots; atomic no-overwrite behavior for concurrent `force=false` writers; precision below six decimals for tiny ticks/prices; service-worker shell caching restricted to the canonical HTML root; and FRED regular-session timestamp/early-close disclosure. Final product/release documentation also still needs reconciliation for full broker identity, legacy broker restore rules, single-envelope archives, and the real browser checklist. Keep generated/ignored `dist/`, dependencies, build-info files, credentials, and `src/data/scenarios/sp500-covid-2020-fred/` out of any commit.
- Next: Resume by inspecting the four completed session/history files and running the combined focused integrity suites. Implement the five release/data findings with regressions, rerun a final repo-wide miss hunt, then run full `npm run check`, audit, ECB byte-for-byte reproducibility, PWA/bundle gates, and `git diff --check`. Finish clean production-browser acceptance on desktop and mobile including refresh/resume, empty console, and offline reopen; update release docs/checklist and this handoff; only then commit intended files to `main`, push `origin/main`, and confirm CI green before declaring V2 complete.

## 2026-07-16 03:43 +03 - Codex (V2 completion paused for shutdown)

- Task: Stopped the V2 completion pass immediately at the user's request. Interrupted the only remaining read-only identity/persistence audit; the evidence, data/docs, and UI integration slices had already finished. Confirmed that no project Vite, Vitest, preview, test, or check process is running.
- Changed: Preserve the current shared worktree exactly as-is. The branch is `main`; `HEAD` and `origin/main` are still `3d7998d`. There are 76 tracked modifications and seven untracked source/test files. Since the 03:27 checkpoint, the combined tree has been compiled and its evidence, data-version documentation, report/history recovery, and App/session fixtures have been reconciled. No commit or push was made because the final product contract and release gates remain incomplete.
- Verified: Root passed `npx tsc -b --pretty false`; the combined session/coach/evidence/archive/library/App integration slice passed 154/154 tests; the importer/release slice passed 59/59 tests; `npm run lint` and `git diff --check` passed. The UI integration agent separately reported 55/55 App/focused tests, the evidence slice 21/21 tests, and the data/docs slice 40/40 scenario/data-version tests with their scoped checks green. There is still no fresh full `npm run check`, dependency audit, final reproducibility proof, live browser acceptance, final second audit, commit, push, or CI result for this worktree.
- Memory: One release-blocking product-integrity gap remains confirmed: a practice-track unit does not yet carry an exact broker mode/config fingerprint, and `ledgerAttemptCompletesTrackUnit` does not compare one. An attempt run under `ideal` or another broker configuration can therefore earn credit for a track intended for the scenario broker. Add exact broker identity to track units, catalog validation, runtime credit checks, fixtures/tests, UI disclosure where needed, and docs. The interrupted identity/persistence audit returned no final report, so rerun that miss-hunt after the fix. Keep generated/ignored `dist/`, dependencies, build-info files, credentials, and `src/data/scenarios/sp500-covid-2020-fred/` out of any commit.
- Next: Resume with the track broker-identity fix and regressions, then rerun the focused track/coach/evidence/session/App/library suites. Complete full check, audit, ECB byte-for-byte reproducibility, and diff gates; perform clean desktop/mobile/refresh/resume/console/offline browser acceptance; run the final repo-wide audit; update release docs and this handoff; only then commit intended files to `main`, push `origin/main`, and confirm CI green before declaring V2 complete.

## 2026-07-16 03:27 +03 - Codex (V2 integration paused for shutdown)

- Task: Stopped immediately at the user's request so the computer can be shut down. All three final-audit agents have completed, and there is no project Vite, Vitest, preview, or test process running. Only the Codex desktop kernel remains active.
- Changed: Preserve the current shared worktree exactly as-is. `main`, `HEAD`, and `origin/main` remain at `3d7998d`; there are 76 tracked modifications and seven untracked source/test files. Since the 03:04 checkpoint, the working tree gained full replay-contract `dataVersion` hashing, strict broker-config fingerprints across run/ledger/session/coach/evidence work, session schema v4, stricter source/rubric/track/transfer identity, malformed-archive recovery, visible report-time archive-save warnings, exact post-game fallback identity, unique history action labels, stronger Surprise masking/import exclusion, and navigation-independent preview/no-credit disclosure. This integration is unfinished and uncommitted; no commit or push was made.
- Verified: The data-version agent reported 6/6 focused tests plus scoped lint/TypeScript/diff checks green. The track/coach agent reported 30/30 focused tests plus scoped lint/diff checks green. The UI audit's pre-fix slice passed 76/76. Root separately passed the new broker fingerprint slice (16 tests plus lint). These are not a valid combined-tree result because root changed shared coach/UI/domain files afterward. No fresh combined TypeScript run, focused integration suite, full `npm run check`, audit, browser acceptance, commit, push, or CI exists for this checkpoint.
- Memory: Current ECB replay-contract hashes are EURGBP `sha256:38289534a70d09d862de6fbc1099a2083d3bce53f9754b7d1310f5003c8adc6e` and EURUSD `sha256:f87c1cc97fdb3bffc4daf18b3191f2c56daa5d9dfe2c5dee2f528f110938f63e`; old observation-only hashes remain reviewed aliases. The last edit was the evidence-profile test migration: `DEFAULT_RUBRIC_FINGERPRINT`, `DEFAULT_BROKER_FINGERPRINT`, and reviewed-source fields are present, but that file and its callers have not been compiled or tested. Treat every recent root integration edit as WIP until inspected. Keep generated/ignored `dist/`, dependencies, build-info files, credentials, and `src/data/scenarios/sp500-covid-2020-fred/` out of any future commit.
- Next: Resume by inspecting the current diff and running `npx tsc -b --pretty false`. Finish evidence-profile regressions/fixtures for exact rubric, source fidelity, sample/review state, and broker fingerprint; update every `previousComparablePracticeScore` call; replace the two stale ECB assertions in `src/data/scenarios/scenarios.test.ts`; and fix any session/coach/App fixture errors. Then run focused broker/session/coach/evidence/track/archive/App/history/library/report suites, followed by full check, audit, reproducibility, and diff gates. Update docs for replay-contract identity, reviewed migrations, legacy behavior, archive recovery, Surprise eligibility, and session v4; complete clean desktop/mobile/refresh/resume/console/offline browser acceptance; run one final repo-wide miss hunt; only then commit intended files to `main`, push `origin/main`, and confirm CI green.

## 2026-07-16 03:04 +03 - Codex (V2 completion paused for shutdown)

- Task: Stopped the Personal Decision Gym V2 completion pass at the user's request. Interrupted the remaining evidence/identity and UI product audit agents and confirmed that no project Vite or Vitest process is running. The Codex desktop kernel process is intentionally left alone.
- Changed: Preserve the shared worktree exactly as it is. `main`, `HEAD`, and `origin/main` remain at `3d7998d`; the checkpoint has 72 tracked modifications and seven untracked source/test files. Since the 02:41 checkpoint, the archive/history integrity fixes, practice-flow fixes, and release/import hardening have been integrated in the working tree. No commit or push was made because the combined tree is still incomplete and has not passed the final product gates.
- Verified: Archive/history passed its focused 88-test slice and a broader 114-test audit slice; the practice-flow agent reported 145 focused tests plus lint/TypeScript/diff checks green; the release/import agent reported 59 focused tests, lint/TypeScript/build/bundle/PWA checks green, and byte-identical live ECB reproductions for EURGBP (152 observations) and EURUSD (104 observations). These are slice-level results only: the current combined worktree has not run a fresh full `npm run check`, dependency audit, browser acceptance, final second audit, or CI.
- Memory: Six release-relevant evidence-identity gaps remain open. (1) ECB `dataVersion` hashes only the observation snapshot, not the full replay contract containing events, instruments, broker, calendar, and corporate actions; the computed full-contract candidates are EURGBP `7e3bbb7cac50ce072bdda3ca080d4f9c88ec4f98994e7b1bf8807701ff11c675` and EURUSD `9412fe8af3af5b7798f584b7cfd7b839d727586d9fc4e0493d0b00b2c9dab072`, with existing snapshot versions retained only as reviewed aliases. (2) Broker presets have no deterministic config fingerprint across session, archive/ledger, coach, trend, and track identity. (3) Fingerprint-less legacy explicit-link assessments can still contribute evidence because the score gate does not require a stored consistent rubric fingerprint. (4) Track catalog validation/evaluation does not defensively forbid a synthetic/sample unit from becoming `validated`. (5) Coach transfer completion is keyed too broadly and can treat a different authored competency/rubric on the same scenario as completion of built-in Event Discipline. (6) validated-source coverage drops expected fidelity/sample/source-reviewed scope, allowing an imported or legacy mismatch with the same id/version to raise confidence. The UI audit was interrupted before it returned findings.
- Next: Resume with those six identity fixes and regressions, including documentation of reviewed migrations and full replay-contract hashes. Then rerun the combined focused suites and complete `npm run check`, `npm audit --audit-level=low`, ECB reproducibility, and `git diff --check`; restart a fresh preview and finish clean desktop/mobile/refresh/resume/console/offline browser acceptance; run the final repo-wide miss hunt; update the release checklist and this handoff; commit all intended source/docs/tests to `main`, push `origin/main`, and confirm CI green. Keep `dist/`, dependencies, build-info files, credentials, and `src/data/scenarios/sp500-covid-2020-fred/` out of the commit.

## 2026-07-16 02:41 +03 - Codex (V2 completion stopped for shutdown)

- Task: Stopped the Personal Decision Gym V2 completion pass immediately at the user's request. Interrupted the archive/history, practice-flow, and release-surface agents; sent Ctrl-C to both local preview sessions; and confirmed that no project Vite/Vitest process remains active.
- Changed: Preserve the entire current worktree without resetting or splitting it. `main`, `HEAD`, and `origin/main` are still at `3d7998d`; there are 71 tracked modifications and seven untracked source/test files. Since the 02:18 checkpoint, the shared diff gained reviewed scenario-data-version migration, stricter archive alias/canonical-corruption guards, and release/import hardening for ECB series keys, documented camelCase CSV headers, safe output roots, complete text-bundle scanning, and FX tick-size/version handling. The practice-flow agent was interrupted while implementing its owned fixes, so treat those shared-file edits as in progress until inspected and tested. No commit or push was made.
- Verified: The most recent complete `npm run check` passed 623 tests before the latest archive, practice-flow, and release/import edits. After that baseline, the data-version slice passed 116 focused tests plus lint/TypeScript/diff checks, and the release/import slice reported 57 focused tests plus lint/TypeScript/diff and bundle/PWA syntax checks green. The newest archive assertion/storage patch and the combined current worktree have **not** received a valid focused/full green run. Browser acceptance is still incomplete. No current CI result exists.
- Memory: A final archive audit found one additional product-integrity blocker: `recordCompletedPracticeRunAtomically` can sort and slice a newly completed run's ledger entry out when 250 prior entries have newer or future-skewed timestamps, while still reporting `ledgerBackfilled: true`. The new local attempt must be forcibly retained and covered by a cap regression. Also verify the latest cross-layer identity assertion for duplicate run and ledger aliases, legacy invalid-aggregate preservation, and the old per-layer readers/writers; the just-added imports may expose a runtime cycle. Practice-flow findings to inspect/finish are empty authored `requiredFields`, reset losing a mandatory checkpoint at replay index 0, coach comparability omitting `dataVersion`/`brokerMode`, and misleading “new regime” wording after all regimes are complete.
- Next: Resume by reviewing the interrupted practice-flow diff, compiling/testing the latest archive patch, fixing the 250-entry retention blocker, and adding the outstanding archive regressions. Then reconcile all agent changes and run the complete `npm run check`, `npm audit`, both ECB reproducibility checks, and `git diff --check`; rebuild and finish clean desktop/mobile/refresh/console/offline browser acceptance. Only after a final second audit is clean should the release checklist and this handoff be finalized, all intended source/docs/tests committed to `main`, pushed to `origin/main`, and CI confirmed green. Keep ignored/generated `dist/`, build-info files, dependencies, credentials, and `src/data/scenarios/sp500-covid-2020-fred/` out of the commit.

## 2026-07-16 02:18 +03 - Codex (V2 completion checkpoint; stopped for shutdown)

- Task: Stopped the active Personal Decision Gym V2 completion pass at the user's request so the computer can be shut down. All three read-only audit agents were interrupted, both local preview servers were stopped, and the in-app browser session was finalized. No background work remains active.
- Changed: Preserve the current uncommitted worktree: 68 tracked files are modified and five source/test files are new (`scripts/import-ecb-eurgbp.test.ts`, `scripts/scenario-import-utils.mjs`, `scripts/scenario-import-utils.test.ts`, `src/data/scenarios/dataVersions.ts`, and `src/domain/history/practiceArchiveEnvelope.ts`). The implementation now includes the canonical atomic practice archive and legacy migration, exact drill/scenario/rubric identities, authored `primarySymbol`, fail-closed legacy-session restore, explicit checkpoint-event selections with provenance-gated assessment/evidence/track credit, honest surprise local self-tests, content-addressed and rollback-safe importers, mixed-fidelity disclosure, and stricter service-worker shell caching. Related V2/privacy/release/data/product docs are updated in the same diff.
- Verified: A combined `npm run check` passed lint, 622/622 tests across 47 files, TypeScript, production build, license scanning, and PWA validation after the explicit-linkage implementation; `npm audit` reported zero vulnerabilities. Later small Drill Debrief/Post-game/archive/doc changes passed their focused 77-test slice plus TypeScript and `git diff --check`, but the full check has **not** been rerun after those latest edits. Browser acceptance was started on clean preview origins but was stopped before the end-to-end drill, mobile, refresh, console, and offline-reopen checks were completed. No commit, push, or CI run exists for this checkpoint.
- Memory: The second audit confirmed one real remaining product-integrity gap: `src/components/history/RunHistory.tsx` still counts and displays legacy or provisional `practiceAssessment.overallScore` values as assessed, although the debrief, post-game, evidence, coach, and track layers correctly require `eventLinkageEvidenceVersion: 1`. A lower-severity accessibility follow-up is to associate the checkpoint-link guidance with its checkbox group using visible ordering and/or `aria-describedby`. Generated/ignored `dist/`, build-info files, dependencies, and `src/data/scenarios/sp500-covid-2020-fred/` must stay out of the commit.
- Next: Resume by fixing Run History's assessed/count/card semantics and adding legacy/incomplete regression coverage; adjudicate the checkpoint-link accessibility follow-up; collect/redo the interrupted second-audit pass; rerun focused tests and then the complete `npm run check`, audit, importer reproducibility, and `git diff --check`; rebuild and finish clean desktop/mobile/refresh/console/offline browser acceptance. Then replace the temporary pause entries with one final handoff, commit the intended source/docs on `main`, push `origin/main`, and confirm CI green before declaring V2 complete.

## 2026-07-16 02:17 +03 - Codex (second audit paused for shutdown)

- Task: Stopped the read-only second product-completeness audit immediately because the user is shutting down the computer. This audit had traced explicit checkpoint-event linkage from the dialog through App/store persistence, assessment, report/debrief, evidence, coach, and track credit; it also checked `primarySymbol`, rubric fingerprints, and legacy `dataVersion` intersections.
- Changed: No product code was changed by this audit. This handoff entry only records the partial review state; preserve the existing uncommitted implementation work described below.
- Verified: The explicit-linkage path was inspected through the domain layers, but no focused or full test command was run in this audit before the stop. One concrete open finding remains: `src/components/history/RunHistory.tsx:23-26`, `:63-70`, and `:150` display and count any stored `practiceAssessment.overallScore`, so legacy assessments without `eventLinkageEvidenceVersion: 1` and incomplete/provisional assessments can still appear as fully assessed in history even though Drill Debrief and Post-game Report intentionally suppress or qualify them. Candidate low-severity accessibility follow-up: `src/components/practice/EventCheckpointDialog.tsx:195-205` presents the linkage checkboxes before the optional/no-link guidance at `:216-220`, and the controls are not associated with that guidance through `aria-describedby`.
- Memory: No automatic-credit leak or forged-subset path was confirmed in the inspected flow. Explicit links are validated as unique visible checkpoint subsets, legacy assessments are excluded from evidence/coach/track credit by the provenance marker, and track completion additionally requires the exact rubric/scenario identity and thresholds. These conclusions are partial until the second-pass tests and miss-hunt are completed.
- Next: Resume by fixing or adjudicating the Run History score semantics and adding legacy/incomplete coverage in `RunHistory.test.tsx`; decide whether to move/associate the optional-link guidance for screen readers; then run the focused linkage/persistence/debrief/history/evidence/coach/track suites and complete the second-pass audit before the full product verification described below.

## 2026-07-16 01:43 +03 - Codex (all work stopped for shutdown)

- Task: Stopped the Personal Decision Gym V2 completion pass immediately so the computer can be shut down. The archive, domain/acceptance, and importer/release agents are all interrupted; no background command or review remains active.
- Changed: The worktree is intentionally uncommitted and now spans 43 tracked files plus three new source files: `scripts/scenario-import-utils.mjs`, `src/data/scenarios/dataVersions.ts`, and `src/domain/history/practiceArchiveEnvelope.ts`. In addition to the work described in the two entries below, the live diff contains in-progress single-envelope archive reads/writes and aggregate App initialization; atomic finished-run recording; authored-drill `primarySymbol` enforcement; rubric-content fingerprints for evidence/track isolation; completed-assessment-only evidence; truthful surprise Blind/Challenge self-tests; and partial content-hash/staged-write importer work. Preserve the combined shared-file edits in `App`, `ScenarioLibrary`, archive/history, practice domain, and session store.
- Verified: `git diff --check` passes at this stop point. Earlier focused suites had passed for the archive/history/App, reports, scenarios, drills, evidence, tracks, and session-store slices, but agents changed shared files after those runs and their final combined test commands were interrupted. Therefore the **current worktree has no valid full green test claim**. No final `npm run check`, importer reproducibility pass, browser acceptance, dependency audit, commit, push, or CI verification has been completed.
- Memory: Resume from this top entry and the current diff; do not discard, reset, or split the uncommitted shared work mechanically. Keep ignored/generated `src/data/scenarios/sp500-covid-2020-fred/`, `dist/`, build-info files, and dependencies out of the commit.
- Next: First inspect the combined diff and run focused archive/domain/App/importer tests. Finish four known product-contract gaps: (1) explicit user-selected checkpoint event linkage rather than automatic credit, including persistence/reporting; (2) fail-closed restore for legacy active sessions that do not identify the exact current scenario `dataVersion`; (3) complete content-derived importer versions, FRED mixed-fidelity disclosure, EUR/GBP parity validation, and staged two-file rollback tests; and (4) make rubric-content identities distinguishable in the evidence UI. Then reconcile privacy/V2/release/data docs, run the complete check/audit/reproducibility gates, perform desktop/mobile/refresh/console/offline browser acceptance, do a second repo-wide audit, replace these pause notes with a final handoff entry, commit to `main`, push `origin/main`, and confirm CI green.

## 2026-07-16 01:41 +03 - Codex (importer work paused at user request)

- Task: Paused the importer/data-fidelity hardening slice immediately because the user is shutting down the computer.
- Changed: Added the untracked `scripts/scenario-import-utils.mjs` scaffold with canonical JSON SHA-256 helpers and a staged two-file `index.ts`/`README.md` replacement routine with rollback. Added mixed/derived/synthetic sample-data disclosure logic to `src/components/scenario/ScenarioLibrary.tsx`; that shared component now also contains another agent's surprise-mode work, so preserve and rebase around the combined diff. No importer has been wired to the new helper yet.
- Verified: No focused tests, TypeScript, lint, or reproducibility run was performed after this partial importer work. The earlier 565/566 status in the next entry remains the latest broad result and is not evidence for this new helper.
- Memory: Do not commit generated or ignored market data, especially `src/data/scenarios/sp500-covid-2020-fred/`. Keep content identities independent of `generatedAt`; FRED should use imported-content SHA-256 plus the base S&P 500 scenario `dataVersion` so authored event changes invalidate the composite version.
- Next: Resume by testing `scenario-import-utils.mjs` for stable hashing, successful paired replacement, forced-write behavior, and rollback after the second rename fails. Then wire content hashes and staged output into `import-local-ohlcv.mjs` and `import-fred-sp500.mjs`; mark FRED as mixed fidelity with explicit observed/derived fields; bring EUR/GBP importer validation, timeout, ordering/range, duplicate, and malformed-quote behavior to EUR/USD parity; update snapshot/content versions and docs; finally run importer-focused tests, TypeScript, lint, reproducibility, and the complete product check. Remove the unused buffered `readFile`/`contents` state from the helper if it remains unnecessary.

## 2026-07-16 01:22 +03 - Codex (paused at user request)

- Task: Paused the repo-wide Personal Decision Gym V2 completion pass because the user is shutting down the computer. All review agents were stopped; no background work remains running.
- Changed: Work is intentionally left uncommitted. The current worktree contains the canonical single-envelope practice archive and legacy migration/fallback work; cross-layer run/ledger conflict guards; stricter completed-assessment evidence rules; authored-drill `dataVersion` enforcement; bundled-scenario version constants; honest user-scenario removal persistence feedback; guided-drill keyboard-mode containment; critical service-worker shell caching; truthful BTC source wording; and a partially completed process-score addition to shared report text. Related product/release/authoring/data docs are also in progress.
- Verified: Archive/history/App focused coverage passed 72/72 with TypeScript, scoped lint, and diff checks. Earlier focused suites for archive parsing/merging, evidence profiles, authored drills, bundled scenarios/tracks/session restore, guided-drill keyboard behavior, and user-scenario persistence passed. The latest full test run is **565/566**: `src/components/report/PostGameReport.test.tsx` still fails because the native-share test expects a practice-process score while that test renders `neutralReport` without a practice assessment. No final full `npm run check`, browser acceptance, audit, commit, push, or CI verification has been performed for this worktree.
- Memory: Resume from this entry and the current diff; do not discard or overwrite the uncommitted work. The generated `src/data/scenarios/sp500-covid-2020-fred/` directory is local/ignored data and must not be committed.
- Next: First fix the `PostGameReport` native-share fixture/expectation and rerun its focused test. Then finish the open V2 product gaps: enforce authored-drill `primarySymbol`; prevent incompatible rubrics sharing one evidence identity (rubric fingerprint); make event-linkage evidence explicit instead of automatically attaching all checkpoint events; decide and document legacy-session restore behavior; harden local/FRED and EUR/GBP importer versioning, fidelity metadata, parity tests, and staged write/rollback behavior; resolve the honest blind/challenge-mode product claim; update privacy/release/V2 docs for the canonical archive envelope. Finally run the complete check and importer reproducibility gates, perform desktop/mobile/refresh/console/offline browser acceptance, do a second audit pass, update this handoff, commit to `main`, push `origin/main`, and confirm CI is green.

## 2026-07-14 15:09 +03 - Codex

- Task: Completed Personal Decision Gym v2 as a coherent local product, including the full practice loop rather than a feature preview.
- Changed: Shipped four versioned Event Discipline drills, the second source-reviewed ECB scenario, immutable practice-session identity, measured Practice Coach assignments, a 250-entry sanitized ledger, competency evidence/trends, exact-version tracks, detailed full-report drill debriefs, strict atomic V2 archive portability with V1 migration, and runnable scenario-authored drills. Added cross-layer guards so malformed imported records cannot claim evidence or track credit without an executed decision and a fully measured completed assessment. Updated V2/release/privacy/authoring docs and bumped the package to `0.3.0`.
- Verified: Clean `npm ci`; final `npm run check` passed lint, 546/546 tests across 45 files, TypeScript, production build, restricted-data/license boundary, and PWA validation; `npm audit` found 0 vulnerabilities; ECB EUR/USD import reproduced 104 observations and its 8 importer tests passed; `git diff --check` passed. The current Codex session had no attached browser runtime, so live desktop, 390 px, refresh, console, and offline-reopen acceptance were not rerun; those checks remain open in the release checklist.
- Memory: none; no repo semantic-memory system is installed. Durable product, evidence, privacy, session-identity, archive, and authoring contracts are documented under `docs/`.
- Next: No known code-level V2 product blocker. Attach a browser and complete the outstanding live desktop/mobile/refresh/console/offline pass, then run the manual Pages workflow from `main` and smoke-test installation on the deployed HTTPS URL.

## 2026-07-13 19:02 +03 - Codex

- Task: Defined V2 as the local-first Personal Decision Gym and started it with a working Local Practice Coach vertical slice.
- Changed: Added a deterministic coaching domain that turns report evidence into a focused next practice; introduced the Decision Foundations milestones, first-run baseline, measurable targets, source/sample/rubric provenance, safe scenario fallback, coach UI, report-to-practice CTA, and briefing focus handoff; hardened malformed v1 recommendation data and kept historical-report actions from pointing at a newer run; documented the V2 user, product loop, boundaries, success criteria, architecture, privacy posture, and delivery sequence.
- Verified: `npm run check` passed lint, 409/409 tests across 32 files, TypeScript, production build, restricted-data scan, bundle-license validation, and PWA asset validation; `npm audit` reported 0 vulnerabilities; `git diff --check` passed. In-app browser QA covered the clean first-run coach, EUR/GBP Explorer briefing focus, replay-to-report-to-next-practice loop, source evidence and target rendering, 390x844 responsive layout with no horizontal overflow, and an empty warning/error console.
- Memory: none; no repo semantic memory is installed. Durable V2 decisions are captured in `docs/v2-personal-decision-gym.md` and the updated product, roadmap, architecture, and privacy docs.
- Next: Build the EUR/GBP Brexit Event Discipline drill with required pre-trade plans, high-importance checkpoints, Hold/Reduce/Exit/Wait decisions, a process-only rubric, and comparable-attempt evidence; then add a versioned evidence ledger and confidence-aware practice tracks.

## 2026-07-13 17:44 +03 - Codex

- Task: Completed Market Time Machine Local Lab v1 as an end-to-end local-first product, beyond bug remediation.
- Changed: Added the scenario library and guided briefing; shipped the official ECB EUR/GBP Brexit lab; introduced structured decision plans, major-event pauses, chronological planned-versus-actual reports, scoring, recommendations, sharing/print/export, local run history and comparisons, user-scenario import/removal, session restore, PWA/offline packaging, release automation, privacy/support/security guidance, and honest Local Lab product boundaries.
- Verified: `npm ci`, `npm run check`, and the complete `npm audit` passed after the Vite/Vitest toolchain refresh: lint, 397/397 tests across 30 files, TypeScript, production build, restricted-data scan, PWA asset validation, and 0 dependency vulnerabilities. The ECB importer reproduced all 152 committed observations byte-for-byte. Browser QA covered the full decision-to-report flow, local history, 5-decimal FX display, event auto-pause cleanup, desktop/mobile layouts with no horizontal overflow, an empty app warning/error console, and a real Chrome offline reopen after the preview server was stopped.
- Memory: none; no repo semantic memory is installed. Durable product decisions and remaining hosted-platform boundaries are documented in the product, roadmap, privacy, and release guides.
- Next: No Local Lab v1 product blocker. Optional hosted-platform work is accounts, managed cohorts, server-authoritative anti-cheat/leaderboards, and broker integrations; public release still requires manually running the Pages workflow and checking install behavior on the deployed HTTPS URL.

## 2026-07-13 16:36 +03 - Codex

- Task: Completed the repo-wide remediation of replay, execution, portfolio, analytics, reporting, persistence, data import, and UI behavior after a full functional audit.
- Changed: Rebuilt replay around a unified multi-symbol timeline; completed partial-fill, liquidity, order-lifecycle, margin, financing, and corporate-action handling; made persisted sessions validated and canonically recomputed; added Explorer/Professional/Challenge behavior and safe confirmations; expanded report scoring, diagnostics, recommendations, and provenance; hardened scenario/import validation; and isolated local licensed scenarios from production bundles.
- Verified: `npm run check` passed lint, 359/359 tests across 26 files, TypeScript, the Vite production build, and the bundle-license scan; `git diff --check` passed. In-app browser QA covered market fill, stepping, journal entry, reset cancellation/confirmation, Challenge masking and locks, full replay completion, report contents, desktop/mobile layouts at 1440x900 and 390x844, zero horizontal overflow, and an empty warning/error console.
- Memory: none; no repo semantic memory is installed. Durable lessons are captured here: use one chronological timeline across symbols, never trust persisted derived state, and keep non-redistributable scenario modules behind a production build boundary.
- Next: No known release blocker. Optional follow-ups are adding a properly licensed full-resolution market-data pack and running the browser flow in CI.

## 2026-05-07 23:39 - Codex

- Task: Completed curated event/news attribution coverage for the BTC scenario and made event sourcing visible in the app.
- Changed: Added `source`/`sourceUrl` to all 14 BTC 2020-2021 events, added event-source coverage helper/tests, surfaced `14/14 sourced` in the event timeline header, rendered source links on visible event rows, and added validator warnings for missing event attribution. Updated scenario authoring docs and changelog.
- Verified: `npm run test -- eventCoverage EventTimeline scenarios scenario` passed 56/56 focused tests; `npm run check` passed lint, 224/224 tests, and production build. In-app browser smoke at `http://127.0.0.1:5173/` confirmed the event timeline shows `14/14 sourced`, and after skipping to end, source links such as WHO and BLS render.
- Memory: none; no repo semantic memory directory is installed. Durable note: curated shipped events should include both `source` and `sourceUrl`; missing attribution is now a validator warning and should be treated as a content-quality gap.
- Next: Add a stronger source-manifest/audit export in finished reports so scenario provenance can be reviewed outside the live timeline.

## 2026-05-07 22:32 - Codex

- Task: Continued professional-emulator UX by surfacing the replay audit log in the app.
- Changed: Added `AuditTrail` UI with typed/tinted audit event rows, wired it into the bottom dashboard as a Replay audit panel, adjusted bottom-grid styling for four operational panels, and added component coverage. Updated changelog.
- Verified: `npm run test -- AuditTrail` passed 2/2 focused tests; `npm run check` passed lint, 218/218 tests, and production build. In-app browser smoke at `http://127.0.0.1:5173/` confirmed the Replay audit panel and empty state render.
- Memory: none; no repo semantic memory directory is installed. Durable note: audit events are now user-visible during replay, so future audit event messages should stay short, non-secret, and user-facing.
- Next: Add a real E2E harness for placing orders and asserting audit/blotter updates after user interactions; then consider filtering audit events by category.

## 2026-05-07 22:24 - Codex

- Task: Implemented the first professional-emulator pass from the roadmap.
- Changed: Added public order/fill/broker/scenario metadata for TIF, partial fills, gap-aware stops, execution reasons, margin/risk snapshots, audit events, market calendars, source manifests, and corporate actions. Broker/session replay now supports volume-limited partial fills, gap-open stop/target execution, TIF expiry, replay audit logs, borrow costs, margin snapshots, and forced liquidation fills. UI now surfaces risk/margin, chart order/fill markers, richer blotter execution details, and execution-quality report metrics. Added `npm run import:ohlcv` for gitignored local licensed OHLCV scenario generation.
- Verified: `npm run check` passed lint, 216/216 tests, and production build. In-app browser smoke opened `http://127.0.0.1:5173/` and confirmed the new risk panel and chart order/fill marker layer were present; ticket order interaction is covered by `TradePanel.test.tsx`.
- Memory: none; no repo semantic memory directory is installed. Durable note: local OHLCV imports generate `src/data/scenarios/local-*/` and are gitignored by default; do not commit generated market data unless redistribution rights are explicit.
- Next: Add a dedicated Playwright E2E harness outside the in-app browser backend for full click-through coverage of placing orders, bracket exits, partial fills, and forced liquidation.

## 2026-05-07 19:02 - Codex

- Task: Continued bracket/OCO workflow polish by making linked exit legs visible in trade history.
- Changed: `TradeHistory` now derives stable `OCO 1/2/...` labels from order groups, displays the same label on bracket legs and their source fill rows, and humanizes filled stop/take-profit order type text. Added chip styling, regression coverage, and a changelog note.
- Verified: `npm run test -- TradeHistory` passed 6/6 focused tests; `npm run check` passed lint, 211/211 tests, and production build.
- Memory: none; no repo semantic memory directory is installed. Durable note: OCO labels are display-only and intentionally derived per render from order `ocoGroupId` plus creation time; raw generated ids are not exposed in the UI.
- Next: Browser QA the full bracket flow: open a position, place a bracket exit, confirm both legs show the same OCO label, then advance until one leg fills and the sibling is cancelled.

## 2026-05-03 17:30 - Codex

- Task: Added bracket/OCO exits for paired stop-loss and take-profit orders.
- Changed: Added optional `ocoGroupId` on orders; pending order requests preserve OCO ids; `submitBracketOrder` creates paired stop-loss/take-profit exits; replay processing cancels sibling pending orders when one OCO leg fills. `TradePanel` exposes a Bracket ticket mode with stop and target inputs. Updated broker docs, roadmap, changelog, and store regression coverage.
- Verified: `npm run test -- sessionStore TradePanel` passed 7/7 focused tests; `npm run check` passed lint, 210/210 tests, and production build.
- Memory: none; no repo semantic memory directory is installed. Durable note: bracket orders currently append stop first, then target, so if both prices are crossed inside the same candle, the stop leg wins and target is cancelled.
- Next: Browser QA for bracket creation and OCO cancellation, then consider showing OCO group labels in trade history.

## 2026-05-03 16:34 - Codex

- Task: Added trade history filters for faster order/fill review.
- Changed: `src/components/journal/TradeHistory.tsx` now has All/Working/Closed tabs with row counts, keeps edit rows adjacent to working orders, and scopes visible rows by filter. Added CSS for the filter control and regression coverage in `TradeHistory.test.tsx`.
- Verified: `npm run test -- TradeHistory` passed 5/5 focused tests; `npm run check` passed lint, 209/209 tests, and production build.
- Memory: none; no repo semantic memory directory is installed.
- Next: Consider browser QA for the full order workflow, then add bracket/OCO behavior for paired stop/target exits.

## 2026-05-03 15:51 - Codex

- Task: Extended working-order edits to stop-loss and take-profit orders.
- Changed: `src/store/sessionStore.ts` now exposes `updatePendingOrder` for limit/stop-loss/take-profit orders while keeping `updateLimitOrder` as a compatibility wrapper. `TradeHistory` can edit either limit prices or trigger prices, and `App` now wires the generalized update action. Added UI/store regression tests for trigger-price editing.
- Verified: `npm run test -- TradeHistory sessionStore` passed 9/9 focused tests; `npm run check` passed lint, 208/208 tests, and production build.
- Memory: none; no repo semantic memory directory is installed.
- Next: Run full check, then browser QA for editing stop/target exits in the table.

## 2026-05-03 15:42 - Codex

- Task: Added stop-loss and take-profit pending order support.
- Changed: `src/types/trading.ts` now includes `stop_loss` and `take_profit` order types with `triggerPrice`. `src/domain/broker/simulator.ts` can place, trigger, and fill limit/stop-loss/take-profit pending orders; old limit-specific helpers remain as compatibility wrappers. `src/store/sessionStore.ts` processes all pending triggerable orders during replay/finish. `TradePanel` exposes Stop and Target order controls. Trade history, docs, changelog, and roadmap were updated for the new order types.
- Verified: `npm run test -- simulator sessionStore TradeHistory` passed 21/21 focused tests; `npm run check` passed lint, 206/206 tests, and production build.
- Memory: none; no repo semantic memory directory is installed. Durable note: stop-loss/take-profit orders currently fill at their trigger price; stop-market slippage is intentionally deferred.
- Next: Add browser QA around opening a position, placing stop/target exits, and finishing the replay; then consider editable trigger prices for stop/target orders.

## 2026-05-03 15:35 - Codex

- Task: Continued order workflow polish by adding edit/replace for working limit orders.
- Changed: `src/store/sessionStore.ts` now validates and updates pending limit order price/quantity while preserving the original order id. `TradeHistory` adds an inline edit form beside cancel controls, with focused styling and regression tests. README, changelog, and roadmap were updated to describe edit/cancel working-order controls.
- Verified: `npm run test -- TradeHistory sessionStore` passed 6/6 focused tests; `npm run check` passed lint, 203/203 tests, and production build.
- Memory: none; no repo semantic memory directory is installed.
- Next: Add stop-loss/take-profit order types or a compact order blotter filter once edit/cancel feels good in browser QA.

## 2026-05-03 15:30 - Codex

- Task: Continued product polish by adding pending limit-order cancellation.
- Changed: `src/store/sessionStore.ts` now cancels working orders and prevents cancelled limits from filling during replay/finish. `TradeHistory` shows all non-filled order statuses and exposes a cancel action while replay is active. `App`, CSS, README, changelog, and roadmap were updated for the visible behavior.
- Verified: `npm run test -- TradeHistory sessionStore` passed 4/4 focused tests; `npm run check` passed lint, 201/201 tests, and production build.
- Memory: none; no repo semantic memory directory is installed.
- Next: Add modify/replace for working limit orders, then consider stop-loss/take-profit order flows.

## 2026-05-03 15:23 - Codex

- Task: Completed GitHub repository metadata hygiene for the open source launch.
- Changed: Added GitHub repository topics, created GitHub Release `v0.1.0`, verified remote only has `main` plus the `v0.1.0` tag, and cleaned issue labels into a focused taxonomy. Updated issue templates to apply `status:needs-triage` and `type:scenario` consistently.
- Verified: `gh repo view` shows topics and latest release; `gh release list` shows `v0.1.0`; `git ls-remote --heads origin` shows only `main`; `git ls-remote --tags origin` shows `v0.1.0`.
- Memory: none; no repo semantic memory directory is installed.
- Next: If dependency automation is re-enabled later, use grouped, low-noise updates and avoid flooding the public branch/PR list.

## 2026-05-03 15:18 - Codex

- Task: Cleaned up GitHub repository hygiene after Dependabot opened too many early alpha update branches.
- Changed: Removed `.github/dependabot.yml` so the public branch/PR list stays focused for the initial open source launch. Planned follow-up in this session: close/delete existing Dependabot PR branches and create/push `v0.1.0` tag.
- Verified: `git ls-remote --heads origin` showed only `main` plus Dependabot branches; `gh pr list` showed 7 open Dependabot PRs. Full code checks were not rerun before this config-only cleanup because the previous `npm run check` passed minutes earlier and no app code changed.
- Memory: none; no repo semantic memory directory is installed.
- Next: Re-enable dependency automation later with grouped updates once the repo has settled.

## 2026-05-03 15:14 - Codex

- Task: Polished the repository for a professional GitHub open source launch.
- Changed: Rebuilt `README.md` into a setup/use/contribution entrypoint; added `LICENSE`, `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `SECURITY.md`, `SUPPORT.md`, `CHANGELOG.md`, CI workflow, Dependabot config, CODEOWNERS, issue templates, and PR template. Updated `package.json`/`package-lock.json` with MIT license, repository metadata, Node engine, keywords, and `npm run check`.
- Verified: `npm run check` passed (`npm run lint`, 199/199 tests, and production build all succeeded).
- Memory: none; no repo semantic memory directory is installed. Durable note: root license is MIT for code/docs, while scenario data/source terms remain declared per scenario metadata and restricted generated data remains gitignored/local-only.
- Next: After GitHub Actions runs on remote, confirm the README CI badge resolves green.

## 2026-05-03 15:04 - Codex

- Task: Added another sample scenario, `kre-banking-crisis-2023`, covering the U.S. regional-bank stress window around SVB, Signature, and First Republic.
- Changed: Added `src/data/scenarios/kre-banking-crisis-2023/index.ts` with deterministic synthetic KRE-proxy candles, FDIC/Fed/OCC/DFPI-sourced event timeline, indicators, benchmarks, and stressed regional-bank broker assumptions. Extended `src/data/scenarios/scenarios.test.ts` with scenario-specific checks.
- Verified: `npm run test -- scenarios` passed 20/20; `npm run test` passed 199/199 across 18 suites; `npm run lint` clean; `npm run build` succeeded.
- Memory: none; no repo semantic memory directory is installed. Durable note: this scenario keeps ETF prices synthetic while event timing/source URLs are anchored to official bank-regulator and central-bank releases.
- Next: Good next additions are `gme-2021-meme-mania` for FOMO/short-squeeze behavior or `gold-2020-2022-inflation` for cross-asset inflation practice.

## 2026-05-03 14:58 - Codex

- Task: Added another sample scenario, `qqq-rate-hike-2022`, covering the Nasdaq/QQQ 2022 inflation and Fed tightening regime.
- Changed: Added `src/data/scenarios/qqq-rate-hike-2022/index.ts` with deterministic synthetic QQQ-proxy candles, official-source Fed/BLS event timeline, indicators, benchmarks, and broker assumptions. Extended `src/data/scenarios/scenarios.test.ts` with scenario-specific checks.
- Verified: `npm run test` passed 195/195 across 18 suites; `npm run lint` clean; `npm run build` succeeded.
- Memory: none; no repo semantic memory directory is installed. Durable note: this scenario intentionally uses synthetic sample ETF prices while event timestamps and summaries are anchored to official Fed/BLS publication times.
- Next: Good next additions are `gme-2021-meme-mania` for FOMO/short-squeeze behavior or `gold-2020-2022-inflation` for cross-asset inflation practice.

## 2026-05-03 14:08 - Codex

- Task: Added a repo-safe FRED SP500 local import path for the COVID scenario.
- Changed: `scripts/import-fred-sp500.mjs` fetches FRED `SP500` closes and generates a gitignored local scenario at `src/data/scenarios/sp500-covid-2020-fred/`. `src/data/scenarios/index.ts` now auto-discovers scenario modules via `import.meta.glob`, so generated local scenarios appear in the switcher without editing registry code. Added `npm run import:fred-sp500`, Vite import-meta typing, gitignore rules, and scenario-authoring docs for the FRED workflow.
- Verified: `npm run import:fred-sp500` generated 253 observations; `npm run test -- scenarios` passed 12/12 before and after generation; `npm run test` passed 191/191 across 18 suites; `npm run lint` clean; `npm run build` succeeded with generated scenario included; `curl -I http://127.0.0.1:5173/` returned HTTP 200.
- Memory: none; no repo semantic memory directory is installed. Durable note: FRED `SP500` output must remain local/gitignored because FRED exposes S&P DJI copyrighted data; the generated scenario uses source closes only, derives OHLC from adjacent closes, and sets volume to 0.
- Next: If a licensed OHLCV source is added later, replace this close-only local importer with a source-true package or keep both as distinct scenario variants.

## 2026-05-03 13:22 - Codex

- Task: Researched and added a second sample scenario, `sp500-covid-2020`, for the S&P 500/SPY COVID crash and recovery training flow.
- Changed: Added `src/data/scenarios/sp500-covid-2020/index.ts` with deterministic synthetic SPY-proxy candles, official-source event timeline, indicators, benchmarks, and broker assumptions. Registered it in `src/data/scenarios/index.ts` and added scenario coverage in `src/data/scenarios/scenarios.test.ts`.
- Verified: `npm run test -- scenarios` passed 12/12; `npm run test` passed 191/191 across 18 suites; `npm run lint` clean; `npm run build` succeeded; `curl -I http://127.0.0.1:5173/` returned HTTP 200.
- Memory: none; no repo semantic memory directory is installed. Durable note: raw S&P DJI/ETF price data was intentionally not redistributed; this scenario uses clearly labeled synthetic sample prices plus official event source URLs.
- Next: Replace sample prices with a properly licensed market-data pipeline when available, and consider adding scenario README/source notes once external package layout exists.

## 2026-05-03 12:57 - Codex

- Task: Fixed four review findings from the UI/UX port.
- Changed: `src/store/sessionStore.ts` now processes pending limit orders before `finish` builds the final report. `TradeHistory` maps fills back to their source orders so filled limits render as limit fills. `ReplayChart` now uses a shared event-overlay helper that only renders chart markers inside the same last-90-candle window as the visible chart. `TradePanel` uses the broker's one-way half-spread estimate. Added regression tests in `src/store/sessionStore.test.ts`, `src/components/chart/ReplayChart.test.ts`, `src/components/trade/TradePanel.test.ts`, and strengthened `TradeHistory.test.tsx`.
- Verified: `npm run test` passed 187/187 tests across 18 suites, `npm run lint` clean, `npm run build` succeeded, `git diff --check` clean, `curl -I http://127.0.0.1:5173/` returned HTTP 200, and in-app browser QA confirmed pending limit + skip-to-end fills and history shows the filled limit order.
- Memory: none; no repo semantic memory directory is installed. Durable fix details are in this handoff entry.
- Next: Add cancel/modify pending orders and a visual screenshot QA pass when the browser screenshot path is healthy.

## 2026-05-03 12:40 - Codex

- Task: Implemented the `finance time machine-2` UI/UX port plan on the existing React/Vite app without replacing replay, broker, portfolio, order, journal, or report domain logic.
- Changed: `src/app/App.tsx` rebuilt the three-zone header, scenario switcher, replay/firewall metadata, derived portfolio/date/day display, and shared chart/timeline event hover state. `src/styles/global.css` now defines the obsidian/amber IBM Plex visual system, responsive dashboard layout, chart overlays, trade ticket, timeline, history, journal, and report styling. `ReplayChart`, `EventTimeline`, `TradePanel`, `TradeHistory`, and `PostGameReport` were redesigned around visible-only data, broker-model clarity, pending/fill state, and the newer narrative report structure. Added focused UI tests for visible event rendering, pending order history, and neutral zero report values.
- Verified: `npm run test` passed 184/184 tests across 15 suites, `npm run lint` clean, `npm run build` succeeded, `git diff --check` clean, `curl -I http://127.0.0.1:5173/` returned HTTP 200. Browser smoke via the in-app browser covered play/pause/step, market buy, pending limit order, limit fill, finish report, and reset to clean start; screenshot capture timed out in the browser tool, so verification used DOM and interaction state.
- Memory: none; no repo semantic memory directory is installed. Durable implementation note is in this handoff entry.
- Next: Add true visual screenshot QA when the browser screenshot path is healthy, then polish mobile breakpoint screenshots, cancel/modify pending orders, and richer scenario packs.

## 2026-05-02 23:38 - Codex

- Task: Continued quality/product pass by adding broker model selection and first pending order workflow.
- Changed: `src/store/sessionStore.ts` now owns active broker config/mode, locks broker selection after first fill, uses selected broker for quotes/execution, and processes triggered pending limit orders during replay advancement. `src/components/trade/TradePanel.tsx` adds Scenario/Ideal/Realistic/Harsh broker controls plus Market/Limit order entry. `src/components/journal/TradeHistory.tsx` shows pending limit orders. `src/domain/broker/simulator.ts` adds limit order placement, trigger detection, and limit fills. `src/domain/replay/engine.ts` accepts active broker spread for tradable prices. Added focused simulator/replay tests and supporting CSS.
- Verified: `npm run test` passed 181/181 tests, `npm run lint` completed cleanly, `npm run build` succeeded, and `curl -I http://127.0.0.1:5173/` returned HTTP 200.
- Memory: active broker config must live in session state, not only in scenario data, so quote spread and execution assumptions remain consistent after preset changes.
- Next: Add cancel/modify pending order controls, stop-loss/take-profit/bracket orders, margin liquidation on replay ticks, and manual browser smoke test.

## 2026-05-02 23:33 - Codex

- Task: Quality pass after alpha vertical slice; integrated previously isolated broker/report modules and tightened first-run polish.
- Changed: `src/domain/broker/simulator.ts` now delegates fill pricing/commission/order validation to broker support modules; `src/domain/broker/orderValidation.ts` validates execution-price buying power and short-opening margin; `src/domain/portfolio/portfolio.ts` now handles short positions and covers; `src/types/reporting.ts`, `src/domain/analytics/behavior.ts`, `src/domain/report/report.ts`, and `src/components/report/PostGameReport.tsx` now expose/render behavioral flags; `src/styles/global.css` removes decorative radial background and adds behavioral signal styling. Added simulator/portfolio tests.
- Verified: `npm run test` passed 177/177 tests, `npm run lint` completed cleanly, and `npm run build` succeeded.
- Memory: resolved prior handoff TODOs for broker helper integration, `BehavioralFlag` report typing, and behavioral flag report rendering.
- Next: Wire actual broker preset selection into UI, add limit/stop orders, add real licensed data/loader, and run a manual browser click-through.

## 2026-05-02 23:24 - Codex

- Task: Final integration verification after one main Claude build agent plus three parallel Claude side agents.
- Changed: No code changes in this entry; verified the integrated app and started the local dev server.
- Verified: `npm run test` passed 174/174 tests, `npm run build` succeeded, `npm run lint` completed cleanly, and `curl -I http://127.0.0.1:5173/` returned HTTP 200.
- Memory: none; no new durable implementation lesson beyond the entries below.
- Next: Click through the browser flow manually, then wire broker presets/behavioral flags into the UI and replace synthetic BTC data with licensed historical data.

## 2026-05-02 23:23 - Claude (vertical-slice integration)

- Task: Stand up the first working vertical slice of Market Time Machine on top of the existing product docs (Vite + React + TS app, lightweight-charts, Zustand, scenario loading, replay engine, broker, portfolio, report, full UI).
- Stack: Vite 5 + React 18 + TypeScript 5, Zustand for session state, lightweight-charts for the candle/volume view, Vitest + Testing Library for tests, ESLint flat config for lint.
- Files touched (created in this slice; some were extended in parallel by other agents):
  - Project scaffolding: `package.json`, `tsconfig*.json`, `vite.config.ts`, `eslint.config.js`, `index.html`, `.gitignore`.
  - Types: `src/types/{market,scenario,replay,trading,reporting,index}.ts`.
  - Domain: `src/domain/replay/engine.ts`, `src/domain/broker/simulator.ts`, `src/domain/portfolio/portfolio.ts`, `src/domain/report/report.ts`, `src/domain/scenario/loader.ts`. Parallel agents added `src/domain/{validation,analytics}/*` and the broker support modules; the report module now delegates metric math to `analytics/*`.
  - Data: `src/data/scenarios/btc-2020-2021/index.ts`, `src/data/scenarios/index.ts`.
  - State + UI: `src/store/sessionStore.ts`, `src/app/App.tsx`, `src/main.tsx`, `src/styles/global.css`, `src/utils/format.ts`, `src/components/{chart,replay,timeline,trade,journal,report}/*`.
  - Tests + setup: `src/test/{setup,fixtures}.ts`, plus colocated `*.test.ts` for replay, broker, portfolio, report.
- Information firewall in code: `src/domain/replay/engine.ts` filters candles, events, indicators, and benchmark points by the visibility rules from `docs/replay-engine.md` (`closeTime`, `publishedAt`, `availableAt`, benchmark `time` all `<= currentTime`). The Zustand store's `selectSnapshot` only ever exposes the visible slice; full-scenario access is reachable only through `buildReport` after `status === "finished"`.
- Demo scenario: `btc-2020-2021` is a deterministic synthetic dataset (mulberry32 RNG, 15 anchor prices shaped to the public BTC 2020-2021 macro path). Clearly tagged `isSampleData: true` and surfaced as a "Sample data" pill in the UI header so contributors do not mistake it for real licensed data. License: `CC-BY-4.0 (sample data)`.
- UI vertical slice: scenario header (title/subtitle, granularity, difficulty, sample-data badge, status pill, replay clock), candlestick chart with volume histogram, Play / Pause / Step / 1x–60x speed control, "Skip to end" + Reset, event timeline that respects `publishedAt`, trade panel with cash / position / total value / total return / realized + unrealized P/L / live spread quote / quantity input / decision-note textarea / Buy / Sell, trade history with attached notes, decision journal, and a post-game report modal with total return, benchmark return, excess return, drawdown, vol, Sharpe/Sortino, fees+spread, slippage, exposure time, best/worst closed trade.
- Verification commands run from repo root:
  - `npm install --no-fund --no-audit` — clean install.
  - `npx tsc -b` — clean type-check, no errors.
  - `npx vitest run` — **174/174 tests across 12 suites passing** (replay visibility, broker spread/commission/rejection, FIFO portfolio, equity-curve + drawdown, behavioral detectors, scenario validation, analytics, BTC scenario sanity).
  - `npx vite build` — production bundle 351 KB JS / 8.4 KB CSS, gzip 111 KB.
  - `npx eslint .` — clean, no warnings.
  - `npx vite --port 5173 --host 127.0.0.1` then `curl http://127.0.0.1:5173/` returned HTTP 200; Vite served `main.tsx`, `App.tsx`, and the store via on-the-fly transform without errors.
- Open risks / next steps:
  1. Ship a real licensed BTC dataset and a JSONL/CSV scenario loader so the lab is not dependent on the synthetic anchor data.
  2. Add a scenario picker UI — the registry already supports multiple scenarios but the shell currently locks to the default.
  3. Surface the analytics module's `behavioralFlags` and an equity-curve mini-chart inside the report modal — both are already computed.
  4. Layer in limit/stop orders and the `ideal` / `realistic` / `harsh` broker presets — execution models, margin, and order-validation modules are already in `src/domain/broker/`.
  5. Wire `assertScenarioValid` from `src/domain/validation/scenario.ts` into a CI step so contributed packages cannot ship with hindsight phrasing or missing `publishedAt` fields.
  6. Browser smoke test — the dev server compiles and serves modules cleanly, but a human should still click through play/pause/buy/sell/finish on a real browser before tagging this slice as "demo-able".

## 2026-05-02 23:22 - Claude (scenario package foundation)

- Task: Strong open-source scenario package foundation per `docs/data-and-scenarios.md` — schema-driven validation utilities, deterministic BTC 2020-2021 demo package, focused tests, contributor docs.
- Changed:
  - `src/domain/validation/scenario.ts` — `ValidationIssue`, `ValidationResult`, and a `validateScenarioPackage` that returns structured errors/warnings (`code` + `path` + `message`). Sub-validators: `validateScenarioMeta`, `validateInstruments`, `validateCandles`, `validateEvents`, `validateBenchmarks`, `validateIndicators`, `validateBroker`. Helpers: `isIsoTimestamp` (regex + `Date.parse`) and `findHindsightPatterns` (8 conservative regex patterns, deliberately precision-biased per advisor guidance).
  - Validation rules cover: ISO 8601 timestamps everywhere; sorted/non-overlapping candles; OHLC bracketing (`high >= max(open,close,low)`); non-negative volume; granularity-gap warnings; candle/event symbol consistency with declared `instruments`; every event has `publishedAt`; `publishedAt` precedes `happenedAt` warning; hindsight phrasing warning on title+summary; events outside scenario range warning; broker license/leverage/spread/commission sanity; slippage-model parameter check; scenario range/license/data-source presence.
  - `src/domain/scenario/loader.ts` — sort is now stable per-symbol; the existing `validateScenario` delegates to the validation module so there is one source of truth. Added `validateScenarioStrict` (full result) and `assertScenarioValid` (throws on errors) for downstream wiring.
  - `src/data/scenarios/btc-2020-2021/index.ts` — split El Salvador into the law-passage event (`evt-2021-06-09-elsalvador-law`, signed) and the in-force event (`evt-2021-09-07-elsalvador`, takes effect) so the schema's `happenedAt`-vs-`publishedAt` distinction is demonstrated with an honest knowable delay rather than fabricated deltas.
  - `src/domain/validation/scenario.test.ts` — 29 unit tests covering ISO parsing, hindsight detection precision/recall, and every error/warning code surfaced by `validateScenarioPackage`.
  - `src/data/scenarios/scenarios.test.ts` — 8 tests asserting every shipped scenario in the registry passes validation with zero errors and the BTC demo has the expected metadata, candle count, event types, benchmarks, broker.
  - `docs/scenario-authoring.md` — contributor guide with quick start, validation checklist, hindsight examples, `happenedAt`-vs-`publishedAt` table, `meta.isSampleData` guidance.
- Verified: `npx vitest run` → 173/173 passing across all 12 suites; `npx tsc --noEmit -p tsconfig.app.json` clean.
- Outside ownership left untouched: `simulator.ts`, app shell, chart UI, report UI, `package.json`, `package-lock.json`. `src/test/setup.ts` and `src/test/fixtures.ts` arrived from another agent mid-session — reused them rather than overwriting.
- Next: register more open-source scenarios (March 2020 SPY crash, 2008 GFC, May 2010 flash crash); wire `assertScenarioValid` into a CI step so a malformed contribution fails fast; consider a structured "post-game reflection" field separate from `summary` so legitimate after-the-fact context never lives where the hindsight detector runs.

## 2026-05-02 23:18 - Claude (broker support modules)

- Task: Implemented professional broker simulation support modules (presets, fill pricing, order validation, margin) per `docs/broker-simulation.md`.
- Changed:
  - `src/domain/broker/executionModels.ts` — `IDEAL_BROKER_CONFIG`, `REALISTIC_BROKER_CONFIG`, `HARSH_BROKER_CONFIG`, `BROKER_PRESETS`, `getBrokerPreset`, `isBrokerPresetName`, `marketFillPrice` (supports `none`/`fixed_bps`/`volume_based`/`volatility_based` slippage models with optional `quantity`/`candleVolumeNotional`/`volatility` context), `slippageBpsFor`, `commissionFor`. Half-spread per side matches `simulator.ts` convention.
  - `src/domain/broker/orderValidation.ts` — `REJECTION_REASONS` constant map + `RejectionReasonCode` union, structured `ValidationResult`, building blocks (`checkQuantity`, `normalizeQuantity`, `checkLotSize`, `buyingPower`, `checkBuyingPower`, `checkLongShortConstraint`, `checkLiquidity`, `checkTradability`) plus a top-level `validateMarketOrder` that short-circuits on the first failure.
  - `src/domain/broker/margin.ts` — `MarginPolicy` type, `IDEAL/REALISTIC/HARSH_MARGIN_POLICY` presets, `MARGIN_POLICY_PRESETS`, `marginPolicyFromBroker` (derives policy from `BrokerConfig`), `initialMarginRequired`, `maintenanceMarginRequired`, `accountEquity`, `marginUtilization`, `marginSnapshot` (single-call view including `isMarginCall`/`requiresLiquidation`/`excessEquity`), `borrowCostFor`, `canOpenAdditionalNotional`.
  - Tests: `executionModels.test.ts` (14), `orderValidation.test.ts` (27), `margin.test.ts` (20). All 61 new tests pass; 7 pre-existing `simulator.test.ts` tests still pass.
- Verified: `npx vitest run src/domain/broker` → 68/68 passing. `npx tsc -p tsconfig.app.json --noEmit` clean for broker modules (unrelated pre-existing errors in `report.test.ts` and `sessionStore.ts`).
- Integration notes (intentionally NOT done — `simulator.ts` is owned by another agent):
  - `simulator.ts` currently inlines a `priceWithSpreadAndSlippage` that mirrors `marketFillPrice` minus the slippage-model branches. It can be replaced by `marketFillPrice(referencePrice, side, broker, ctx)` to gain volume/volatility-aware slippage.
  - `simulator.ts` currently inlines its rejection prose. The `REJECTION_REASONS` map exposes the same strings; the simulator can switch to `rejection("...").message` for consistency without breaking callers.
  - `validateMarketOrder` can replace the procedural validation block in `simulator.executeMarketOrder` once we want a single-source-of-truth for the rejection ladder.
  - Margin is not yet wired into `executeMarketOrder` or the portfolio loop; `marginSnapshot` is ready to be called per replay tick or pre-trade for `marginCallPolicy === "reject_new_orders"` and `"liquidate_on_threshold"`.
- Outside ownership left untouched: `simulator.ts`, scenario files, app shell, chart UI, `package.json`, `package-lock.json`.
- Next: wire the new helpers into `simulator.ts` (pre-trade margin gate, slippage models, structured rejection codes); add limit/stop order types; introduce instrument tradability and market-hours wiring (`checkTradability` accepts `marketOpen` already).

## 2026-05-02 23:20 - Claude (analytics & report foundation)

- Task: Implemented the analytics/reporting foundation per `docs/analytics-and-reporting.md`. Pulled metric calculations out of the inline `report.ts` and added a behavioral-flag detector layer.
- Changed:
  - `src/domain/analytics/metrics.ts` — pure functions: `totalReturn`, `benchmarkReturn`, `excessReturn`, `simpleReturns`, `mean`, `stdDev`, `volatility`, `annualizedVolatility`, `maxDrawdown`, `sharpeRatio`, `sortinoRatio`, `calmarRatio`, `periodsPerYearForGranularity`. Annualization is parametric (no hardcoded `sqrt(365)`); `periodsPerYearForGranularity` maps the scenario's granularity.
  - `src/domain/analytics/trades.ts` — pure functions: `realizeTrades` (FIFO matching with per-unit commission proration; replaces the buggy proration in the old inline `computeTradeOutcomes`), `tradeOutcomes`, `bestTrade`/`worstTrade` (sign-aware), `winRate`, `profitFactor`, `averageWin`/`averageLoss`, `feesTotal`, `slippageTotal`, `turnover`, `exposureTime` (counts a candle as exposed if held entering OR exiting it).
  - `src/domain/analytics/behavior.ts` — `BehavioralFlag` type + `BehavioralFlagType` union; detectors `detectPanicSell`, `detectFomoBuy`, `detectEarlyProfitTake`, `detectOvertrading`, and an aggregator `detectAllBehavioralFlags`. Each detector takes explicit candle slices and parameters — no global session access — so future-data exposure is a caller decision (only the finished-session report passes the full-session candles in). Tunable thresholds via `defaultDetectorParams`; severity mapped from magnitude.
  - `src/domain/analytics/index.ts` — barrel export.
  - `src/domain/report/report.ts` — refactored `buildReport` to delegate every metric to `analytics/*`, derive `periodsPerYear` from `scenario.meta.defaultGranularity`, and emit a `behavioralFlags: BehavioralFlag[]` field on the returned payload. Public surface (`buildReport({ scenario, fills, initialCash })`, `buildEquityCurve`) is preserved for `sessionStore.ts`. Return type is now `FinishedSessionReport = ReportPayload & { behavioralFlags }`. The previous inline `maxDrawdown`/`stdDev`/`computeReturns`/`computeTradeOutcomes`/`computeExposureTime` functions are removed.
  - Tests: `src/domain/analytics/metrics.test.ts` (24), `trades.test.ts` (14), `behavior.test.ts` (14). 52/52 pass.
- Verified: `npx vitest run src/domain/analytics` → 52/52. `npx vitest run` → 171/173 (the two failing tests are in `src/domain/validation/scenario.test.ts`, owned by another agent and unrelated to this work). `npx tsc -b` clean. The pre-existing `src/domain/report/report.test.ts` (4 tests) imports `maxDrawdown` from `analytics/metrics` and uses `buildReport` — both still pass against the refactored module.
- Decisions worth flagging:
  - I did not extend `src/types/reporting.ts` to add `BehavioralFlag`. That file is shared and a parallel agent may still be editing it. The type lives in `analytics/behavior.ts` and is re-exported via `analytics/index.ts`. If/when stable, lift it into `types/reporting.ts` and add the `behavioralFlags` field to `ReportPayload` itself.
  - `exposureTime` semantics: a candle counts as "exposed" if the position is held *entering or exiting* it. The previous implementation under-counted candles where the user opened and closed within the same period.
  - `realizeTrades` uses per-unit commission proration (not the old proration formula, which compounded a partially-mutated `lot.commission` against the post-mutation quantity — buggy). Realized P/L now subtracts `commissionPerUnit * used` cleanly.
- Outside ownership left untouched: `package.json`, `package-lock.json`, app shell, chart UI, scenario package files, broker simulator, shared `src/types/*`, `src/test/*`.
- Next: lift `BehavioralFlag` into `types/reporting.ts`; add lesson-generation that summarizes the flag set in plain English; surface behavioral flags in the post-session UI; wire scoring (35/25/20/10/10 weighting from the docs) on top of these metrics.
